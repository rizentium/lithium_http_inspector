import 'package:flutter/material.dart';

class HttpInspectorScreen extends StatelessWidget {
  const HttpInspectorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Lithium Inspector'),
      ),
    );
  }
}
